# 网页的构成

```
html 网页的骨架
css  网页的美化
js   网页的控制台
```

# http协议

```
通过服务端发送请求，获取资源信息
分为以下几种请求方式

GET     请求用于获取数据的
POST    请求用于保存数据的
PUT     请求用于修改数据的
DELETE  请求用于删除数据的
OPTION  这是一个预检请求
以上这种方式称之为rest风格
大部分还是用get和post

#http协议的特性，是一个单工的通信方式

1、用户发起请求
2、访问域名，首先查dns服务器，查看域名绑定的ip

```